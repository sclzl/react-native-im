import { Dimensions } from "react-native";
export const { width: SCREEN_W, height: SCREEN_H } = Dimensions.get("screen");
export const { width: WINDOW_W, height: WINDOW_H } = Dimensions.get("window");
 